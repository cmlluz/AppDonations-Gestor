import 'package:flutter/material.dart';

class InstitutionRegisterPage extends StatelessWidget {
  const InstitutionRegisterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
